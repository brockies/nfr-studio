# React Frontend

This folder contains the React + Tailwind + shadcn-style frontend for the NFR Studio migration.

## Run locally

1. Install dependencies:

```bash
npm install
```

2. Start the frontend:

```bash
npm run dev
```

3. Point it at the Python API if needed:

```bash
VITE_API_BASE_URL=http://localhost:8000
```
